server:
  port: 8081

spring:
  application:
    name: directory-service

  datasource:
    url: jdbc:h2:mem:directorydb
    driverClassName: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
